interface:
  display_name: "Subagent Orchestrator"
  short_description: "主模型规划验收，写入 DeepSeek 优先，明确任务中 Spark 排在 Luna 前"
  default_prompt: "使用 $subagent-orchestrator 让主模型负责规划与验收，只读定向取证优先 Spark，写入按 DeepSeek、Kimi、Spark、Luna、Terra 顺序委派，Spark 不适用或不可调用时跳过，并记录任务用量与恢复次数。"
policy:
  allow_implicit_invocation: true
