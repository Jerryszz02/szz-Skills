interface:
  display_name: "Subagent Orchestrator"
  short_description: "串行委派、按需分工，同一执行器持续推进，减少主模型读取与交接"
  default_prompt: "使用 $subagent-orchestrator，按需要串行安排读代码或实现工作，每次交付一块完整结果并尽量续用原 worker，由主模型保留关键决策与验收，减少主模型用量和总费用。"
policy:
  allow_implicit_invocation: false
